package Class22;

public interface numberFinder {
    //search the array for the number if found return true
    public boolean searchArr(int[] array, int number);
        }

        class Main implements numberFinder{
    @Override
    public boolean searchArr(int[] array, int number){

            return false;
        }
    }

